{
  "name": "diet",
  "version": "0.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "npx cap sync",
    "cap:assets": "npx capacitor-assets generate",
    "cap:prepare": "npm run build && npm run cap:assets && npm run cap:sync",
    "android:apk": "cd android && ./gradlew assembleDebug",
    "android:build": "npm run cap:prepare && npx cap build android && npm run android:apk"
  },
  "license": "MIT",
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.3.1",
    "sass": "^1.98.0",
    "vite": "^7.3.1",
    "vite-plugin-solid": "^2.11.11"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@kobalte/core": "^0.13.11",
    "dexie": "^4.4.2",
    "solid-js": "^1.9.11"
  }
}